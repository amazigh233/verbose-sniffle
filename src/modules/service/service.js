"use strict";

// Compatibility boundary while the domain internals are further decomposed.
// Routes/controllers only depend on this module, never on the legacy file.
module.exports = require("../../service-data");
